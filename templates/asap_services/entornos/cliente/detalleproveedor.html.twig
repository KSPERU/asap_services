{% extends 'base.html.twig' %}
{% block title %}
  Proveedor - Servicios
{% endblock %}

{% block stylesheets %}
  {{ encore_entry_link_tags('app') }}
  <link rel="stylesheet" href="{{ asset('css/cliente/detalle_proveedor.css') }}" />
{% endblock %}

{% block body %}
<div class="container mt-3">
    <div class="prov-container">

        <div>
            <div class="text-end me-2 mb-1">
                <a href='{{ path('app_asap_services_entornos_cliente_inicio') }}'>
                    <svg xmlns="http://www.w3.org/2000/svg" height="1.2em" viewBox="0 0 384 512"><!--! Font Awesome Free 6.4.2 by @fontawesome - https://fontawesome.com License - https://fontawesome.com/license (Commercial License) Copyright 2023 Fonticons, Inc. --><path d="M342.6 150.6c12.5-12.5 12.5-32.8 0-45.3s-32.8-12.5-45.3 0L192 210.7 86.6 105.4c-12.5-12.5-32.8-12.5-45.3 0s-12.5 32.8 0 45.3L146.7 256 41.4 361.4c-12.5 12.5-12.5 32.8 0 45.3s32.8 12.5 45.3 0L192 301.3 297.4 406.6c12.5 12.5 32.8 12.5 45.3 0s12.5-32.8 0-45.3L237.3 256 342.6 150.6z"/></svg>
                <a>
            </div>
            <div class="row d-flex justify-content-center">
                <img class="img-perfil" src="{{ asset('image/' ~ ((proveedor.pfoto is null) ? 'perfil.png' : proveedor.pfoto)) }}" />
            </div>
            <div class="row d-flex justify-content-center">
                <div class="col-12 nombre-proveedor text-center mt-2">
                    <h3 class="fw-bold">{{ proveedor.pnombre ~ " " ~ proveedor.papellido }}</h3>
                </div>
            </div>
        </div>
        <div class="info-prov-container">
            <div class="edad">
                <span>Edad: </span>
                <span> - años</span> {# Necesario agregar campo #}
            </div>
            <div class="sobre-mi">
                <div class="titulo">Sobre mi: </div>
                <p>{{ (proveedor.pbiografia is null) ? '-' : proveedor.pbiografia }}</p>
            </div>
            <div class="titulo-2">
                <span>Contacto: </span>
                <span>{{ (proveedor.pcontacto is null ) ? '-' : proveedor.pcontacto }}</span>
            </div>
            <div class="titulo-2">
                <span>Direccion: </span>
                <span>{{ (proveedor.pdireccion is null) ? '-' :  proveedor.pdireccion }}</span>
            </div>
            <div class="titulo-2">
                <span>Experiencia: </span>
                <span>{{ (proveedor.pexperiencia is null) ? '-' : proveedor.pexperiencia }}</span>
            </div>
            <div class="titulo-2">
                <span>Distrito: </span>
                <span>{{ (proveedor.pdistrito is null) ? '-' : proveedor.pdistrito }}</span>
            </div>
            <div class="habilidades">
                <div class="titulo">Habilidades Tecnicas: </div>
                <p>{{ (proveedor.phabilidades is null) ? '-' :  proveedor.phabilidades }}</p>
            </div>
        </div>
    </div>
    
    {#  
        <form method="POST"> 
            <button type="submit">Enviar</button>
        </form> 
        <form action="{{ path('app_cliente_comunicate_post', {'id': proveedor.id}) }}" method="post">
            <button type="submit">Comunicate</button>
        </form>
        {{ form(form) }}  
    #}
  </div>
            
  {{ include('components/footer.html.twig') }}
{% endblock %}